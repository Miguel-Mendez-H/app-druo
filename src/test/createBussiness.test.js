import React from 'react';
import { render } from '@testing-library/react';
import CreateBusinessPanel from '../views/createBussiness';

test('renders createBussiness component', () => {
    render(<CreateBusinessPanel />);
});
